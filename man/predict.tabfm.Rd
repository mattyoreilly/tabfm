% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabfm.R
\name{predict.tabfm}
\alias{predict.tabfm}
\title{Predict from a TabFM model}
\usage{
\method{predict}{tabfm}(object, newdata, type = c("response", "prob"), ...)
}
\arguments{
\item{object}{A \code{"tabfm"} object returned by \code{\link[=tabfm]{tabfm()}}.}

\item{newdata}{A data frame with exactly the same columns as the training
data.}

\item{type}{What to return: \code{"response"} (the default) for class labels or
numeric predictions, or \code{"prob"} for a matrix of class probabilities
(classification only).}

\item{...}{Ignored, for compatibility with the \link[stats:predict]{predict()}
generic.}
}
\value{
For \code{type = "response"}, a vector with one prediction per row of
\code{newdata}: character class labels for classification, numeric values for
regression. For \code{type = "prob"}, a numeric matrix with one row per row of
\code{newdata} and one column per class, named by class.
}
\description{
Generate predictions — class labels, class probabilities, or numeric
values — from a model fitted with \code{\link[=tabfm]{tabfm()}}.
}
\examples{
\dontrun{
fit <- tabfm(iris[-5], iris$Species)

# class labels
predict(fit, head(iris[-5]))

# class probabilities, one column per class
predict(fit, head(iris[-5]), type = "prob")
}
}
\seealso{
\code{\link[=tabfm]{tabfm()}}

Other modelling:
\code{\link[=tabfm]{tabfm()}}
}
\concept{modelling}
