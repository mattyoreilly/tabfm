% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabfm.R
\name{tabfm}
\alias{tabfm}
\title{Fit a zero-shot TabFM model}
\usage{
tabfm(
  x,
  y,
  task = c("classification", "regression"),
  backend = c("jax", "pytorch")
)
}
\arguments{
\item{x}{A data frame of predictors. Character and factor columns are
handled natively by TabFM; no dummy coding is needed.}

\item{y}{The response: a character vector or factor for classification, a
numeric vector for regression. Must have one value per row of \code{x}.}

\item{task}{Type of model to load: \code{"classification"} (the default) or
\code{"regression"}.}

\item{backend}{Compute backend: \code{"jax"} or \code{"pytorch"}. Must match the
backend you installed with \code{\link[=install_tabfm]{install_tabfm()}}.}
}
\value{
A model object of class \code{"tabfm"}: a list carrying the training
data (\code{x}, \code{y}), \code{task}, \code{backend}, \code{features} (the training column
names), and a cache holding the fitted Python estimator. Use
\link[=predict.tabfm]{predict()} to generate predictions from it.
}
\description{
\code{tabfm()} loads the pretrained TabFM v1.0.0 weights and conditions them on
your training data. Because TabFM learns in-context — a single forward
pass over the table — there is no feature engineering, no hyperparameter
tuning, and no retraining.

Loading the weights is the slow part, so it happens once per session for
each \code{task}/\code{backend} combination and the model is reused by later fits;
repeated calls to \link[=predict.tabfm]{predict()} are cheap.

Fitted models survive \code{\link[=saveRDS]{saveRDS()}}/\code{\link[=readRDS]{readRDS()}}: because a zero-shot fit
holds no learned state beyond the training data, a restored fit is
re-conditioned on its stored data automatically the first time you call
\link[=predict.tabfm]{predict()} in the new session (which also pays that
session's one-time weights load).
}
\examples{
\dontrun{
fit <- tabfm(iris[-5], iris$Species)
fit

predict(fit, head(iris[-5]))
predict(fit, head(iris[-5]), type = "prob")

fit <- tabfm(mtcars[-1], mtcars$mpg, task = "regression")
predict(fit, head(mtcars[-1]))
}
}
\seealso{
\code{\link[=predict.tabfm]{predict.tabfm()}} to make predictions; \code{\link[=install_tabfm]{install_tabfm()}} for
one-time setup.

Other modelling:
\code{\link[=predict.tabfm]{predict.tabfm()}}
}
\concept{modelling}
